---
layout: gtsam-post
title: "FAST-Sync: A Fast Start for Group Synchronization on Any Matrix Lie Group"
---

Blog post by: Firat Taxpulat

Paper authors: Shane Holmes, Yiran Luo, Firat Taxpulat, David M. Rosen, and Frank Dellaert

<style>
.MJXc-display,
.fast-sync-table {
  overflow-x: auto;
  overflow-y: hidden;
}
</style>

* TOC
{:toc}

*Published in IEEE Robotics and Automation Letters, vol. 11, no. 9, pp. 10377–10384, September 2026. [DOI: 10.1109/LRA.2026.3710327](https://doi.org/10.1109/LRA.2026.3710327).*

Group synchronization sounds specialized, but the problem is familiar to many GTSAM users. Pose-graph SLAM estimates global robot poses from noisy relative poses. Rotation averaging recovers camera orientations from pairwise matches. Closely related problems appear in monocular SLAM with scale drift, $SL(d)$-based 3D reconstruction, invariant filtering on $SE_2(3)$, and moving reference frames modeled with the Galilean group.

In each case, we have unknown group elements $X_1,\ldots,X_N\in G$ and noisy measurements $\widetilde X_{ij}\approx X_i^{-1}X_j$. The aim is to recover a globally consistent set of states, up to a common gauge transformation.

**FAST-Sync is a batch initializer for this problem over matrix Lie groups.** It replaces the nonlinear problem with a structured linear approximation, solves that approximation efficiently, projects the answer back to the chosen group, and hands the result to a local optimizer such as Gauss-Newton or Levenberg-Marquardt.

That last step is essential. FAST-Sync is not a replacement for nonlinear optimization; it is a way to start nonlinear optimization in a much better place.

## Why initialization is the hard part

The maximum-likelihood synchronization problem is usually high-dimensional and non-convex. GTSAM's local optimizers can solve large instances efficiently, but—as with local optimization generally—the result depends on the initial estimate. A poor start can require many iterations to repair, or it can place the optimizer in the wrong basin of attraction.

A common baseline is to select a spanning tree and concatenate relative measurements outward from a root. This is very cheap, but every edge contributes error. Along a long tree path, those errors accumulate.

<figure class="center" style="width: 100%; max-width: 520px; text-align: center;">
  <img src="/assets/images/fast-sync/fast-sync-mit-initialization.png"
    alt="Three MIT SE(2) pose-graph trajectories comparing a maximum-spanning-tree initialization, FAST-Sync initialization, and local optimization from FAST-Sync."
    style="width: 100%; display: block; margin-left: auto; margin-right: auto;" />
  <figcaption>Figure 1. A maximum-spanning-tree (MST) initialization can lie outside the global optimizer's basin of attraction. FAST-Sync uses all relative measurements to produce a linear initialization from which local optimization converges to the global optimum on this MIT $SE(2)$ example. Reproduced from Figure 1 of the published paper.</figcaption>
</figure>
<br />

The figure captures the main motivation. The spanning-tree estimate is inexpensive but severely distorted. FAST-Sync's estimate is already close enough that ordinary local refinement reaches the desired solution.

This matters even when a globally certifiable solver is available. A strong initial estimate can reduce the work needed to reach—and then certify—a high-quality solution.

## From a manifold problem to a linear start

FAST-Sync generalizes the philosophy of chordal initialization beyond familiar rotation and rigid-pose groups. Its first move is to replace the original Log-map likelihood with a quadratic Frobenius-norm surrogate:

$$
J_F(X)=
\frac{1}{2}\sum_{(i,j)\in E}
\kappa_{ij}
\left\|X_j-X_i\widetilde X_{ij}\right\|_F^2,
$$

where $\kappa_{ij}=\sigma_{ij}^{-2}$ is the inverse noise variance of measurement $(i,j)$ under the paper's isotropic concentrated-Gaussian model. FAST-Sync then temporarily relaxes the constraints $X_i\in G$, solves for matrices in the surrounding ambient vector space, and projects them back to $G$.

The residual is linear in the unknown matrix entries. Suppressing the scalar row weight, as in the paper's notation, an edge $(i,j)$ defines the sparse block row

$$
(H_{ij})_\ell=
\begin{cases}
-\widetilde X_{ij}^{T}, & \ell=i,\\
I_d, & \ell=j,\\
0_{d\times d}, & \text{otherwise},
\end{cases}
\qquad
\overline H=H\otimes I_d.
$$

The Kronecker product is the key computational observation. For nonunit weights, the corresponding edge rows are scaled by $\sqrt{\kappa_{ij}}$ in the weighted least-squares system. The Kronecker structure replaces the $d^2M\times d^2N$ vectorized system with $H\in\mathbb{R}^{Md\times Nd}$. After appending the gauge row, FAST-Sync factors the reduced, gauge-augmented matrix $A\in\mathbb{R}^{(M+1)d\times Nd}$ and reuses its triangular factor in the column-wise recovery.

A first-order small-noise expansion motivates the Frobenius surrogate. The paper notes that its quadratic approximation is equal to the preceding matrix-space residual for common groups including $SO(d)$, $SE(d)$, and $SU(2)$. For more general matrix Lie groups, it is a practical heuristic for initialization—not an exact replacement for the final manifold objective.

## Let the graph choose the gauge

For a connected synchronization graph, applying one common global transformation to every state leaves all relative measurements unchanged. A linear initializer therefore needs to fix one gauge state, usually to the identity. A disconnected graph would require one gauge per connected component.

In the noiseless problem, the choice of root is algebraically arbitrary. In the noisy ambient-space relaxation, however, the ordering affects both sparse fill and how far intermediate matrices drift from the group before projection.

FAST-Sync uses a **block-preserving Nested Dissection** ordering. The $d\times d$ entries belonging to one state always move together, while Nested Dissection recursively separates the measurement graph using small vertex separators. The final state in the reordered system becomes the identity gauge.

After sparse factorization, the remaining blocks are recovered in reverse order:

$$
\widehat X_N=I_d,\qquad
\widehat X_j=
-\left(
\sum_{k\in\mathrm{nz}_j}
\widehat X_kR_{jk}^{T}
\right)R_{jj}^{-T},
\qquad
X_j^0=\Pi_G(\widehat X_j).
$$

Here, the $R_{jk}$ are blocks of the sparse triangular factor and $\mathrm{nz}_j$ contains the later nonzero blocks needed to recover state $j$. In an implementation, the final multiplication is performed with a triangular solve rather than by forming an inverse.

The ambient estimates $\widehat X_j$ need not lie exactly on $G$, so each is rounded with a group-specific projection $\Pi_G$. For $SO(d)$, the paper uses a polar-decomposition projection. Other groups need their own appropriate projection.

<figure class="center" style="width: 100%; max-width: 1100px; text-align: center;">
  <img src="/assets/images/fast-sync/fast-sync-nested-dissection.png"
    alt="Sparse triangular block structure and elimination dependency graph produced by Nested Dissection for the Statue of Liberty dataset."
    style="width: 100%; display: block; margin-left: auto; margin-right: auto;" />
  <figcaption>Figure 2. Nested Dissection produces the sparse triangular block structure at left and the corresponding back-substitution directed acyclic graph at right. The final root block is fixed to the group identity, and the remaining matrices are recovered in reverse order. Reproduced from Figure 3 of the published paper.</figcaption>
</figure>
<br />

Nested Dissection helps twice: it reduces fill in the sparse factorization, and it tends to choose a central root with a shallower elimination dependency graph. The second benefit is empirical and especially interesting—an ordering normally treated as a speed detail also influences initialization quality. A full theory of that effect remains open.

## Where FAST-Sync fits in a GTSAM pipeline

The published paper names GTSAM as one possible toolkit for the final Gauss-Newton or Levenberg-Marquardt refinement. It does not describe or evaluate a GTSAM implementation of FAST-Sync's reduced solve, ordering, back-substitution, or projection stages. The workflow below is therefore an integration pattern rather than a pipeline tested in the paper:

1. Build the intended GTSAM factor graph from relative measurements, with the correct variable types, noise models, and gauge prior.
2. Pass the same relative measurements and the paper's scalar inverse-variance weights $\kappa_{ij}$ to a FAST-Sync implementation in their matrix form.
3. Compute the block-preserving ordering, reduced sparse factorization, back-substitution, and projection to $G$.
4. If node $r$ has prior value $P$, align gauges by left-multiplying every estimate $X_i^0$ by $P(X_r^0)^{-1}$.
5. When matching GTSAM manifold types and factors exist, convert the projected matrices to GTSAM values and insert them into an initial `Values` container.
6. Refine the original factor graph with `LevenbergMarquardtOptimizer`, `GaussNewtonOptimizer`, or another suitable optimizer.

FAST-Sync solves the ambient-space initialization surrogate; GTSAM then refines the residuals encoded in the final factor graph on the product manifold $G^N$. As of August 6, 2026, no public `FastSync` class or released example is part of GTSAM, so batch integration occurs through the optimizer's initial `Values`.

Stable GTSAM is [4.2.2](https://github.com/borglab/gtsam/releases/tag/4.2.2), while [4.3a2](https://github.com/borglab/gtsam/releases/tag/4.3a2) is an alpha whose new APIs are explicitly unstable. The 4.3 alpha line includes matrix-Lie-group infrastructure, `Gal3`, and `SL4`. Its [`FrobeniusBetweenFactorNL`](https://github.com/borglab/gtsam/blob/4.3a2/gtsam/slam/FrobeniusFactor.h) evaluates $\lVert X_j^{-1}X_i\widetilde X_{ij}-I\rVert_F$, matching the paper's nonlinear Frobenius error in Eq. (5), up to sign, for fixed-size matrix Lie groups. It does not implement FAST-Sync's linear solve, Nested-Dissection gauge choice, or projection.

GTSAM support remains group-dependent: its $Sim(3)$ type is [`Similarity3`](https://github.com/borglab/gtsam/blob/4.2.2/gtsam/geometry/Similarity3.h); `Gal3` and `SL4` are currently alpha-line types; and no named built-in `SU2` type is documented.

That makes FAST-Sync useful in several ways:

- For `Pose2`, `Pose3`, and `Rot3` graphs, it offers an alternative to odometric-chain or spanning-tree initialization.
- The same linear-algebraic template extends to groups used for similarity, projective, and moving-frame estimation, provided a matrix representation and practical projection are available.
- For $SO(2)$ or $SO(3)$ rotation averaging, the projected estimates can also be supplied as the initial `Values` to GTSAM's [`ShonanAveraging`](https://github.com/borglab/gtsam/blob/4.2.2/gtsam/sfm/ShonanAveraging.h). This integration was not evaluated in the FAST-Sync paper.
- One possible deployment pattern, also not studied in the paper, is to use FAST-Sync as a batch bootstrap before first adding a connected graph or submap to iSAM2. In [`ISAM2::update`](https://github.com/borglab/gtsam/blob/4.2.2/gtsam/nonlinear/ISAM2.h), `newTheta` initializes newly introduced variables; it does not reinitialize variables already in the system.
- The paper assumes scalar isotropic edge weights. A GTSAM application may use the resulting `Values` to initialize a final graph with anisotropic or robust noise models, but that is an integration choice, not a result established by the paper.

## What the experiments show

The paper evaluates six groups: $SO(3)$, $SE(3)$, $SU(2)$, $SL(4)$, $Gal(3)$, and $Sim(3)$. In synthetic small-world experiments, maximum-spanning-tree initialization is cheaper at the first step, but accumulated error often leaves much more work for the nonlinear optimizer. FAST-Sync pays more up front and frequently wins on total time because refinement starts closer to the desired basin. For $SO(3)$, $SE(3)$, and $SU(2)$, SE-Sync supplies the certifiably global reference solution. For $SL(4)$, $Gal(3)$, and $Sim(3)$, the paper estimates the global optimum using local optimization initialized from ground truth.

The Nested Dissection ablation makes its computational contribution visible:

<div class="fast-sync-table" markdown="1">

| Group | Chordal | FAST-Sync without ND | FAST-Sync | Reduction vs. no ND |
|:--|--:|--:|--:|--:|
| $SO(3)$ | 46.7 | 33.9 | **28.6** | 16% |
| $SE(3)$ | 79.4 | 67.1 | **50.0** | 25% |
| $Sim(3)$ | — | 143 | **72.2** | 50% |
| $Gal(3)$ | — | 187 | **113** | 40% |
| $SL(4)$ | — | 534 | **333** | 38% |

</div>

*Table 1. Median total runtime in milliseconds over the synthetic grid, adapted from the Speed statistic in Table I of the published paper. The final column is computed from the reported runtimes. The chordal comparison was run only for $SO(3)$ and $SE(3)$.*

On the tested $SO(3)$ and $SE(3)$ problems, FAST-Sync is about 35–40% faster than the off-the-shelf chordal baseline while reaching nearly the same solution quality. Those percentages describe these experiments, not a universal speed guarantee.

<figure class="center" style="width: 100%; max-width: 1100px; text-align: center;">
  <img src="/assets/images/fast-sync/fast-sync-cross-group-results.png"
    alt="Runtime-versus-Frobenius-error curves comparing maximum-spanning-tree initialization and FAST-Sync on Intel, Rim, Pantheon, and drone datasets, plus a connectivity and noise plot."
    style="width: 100%; display: block; margin-left: auto; margin-right: auto;" />
  <figcaption>Figure 3. On representative $SE(2)$, $SE(3)$, $SO(3)$, and $Gal(3)$ datasets, FAST-Sync starts with lower Frobenius error and generally reaches the reference solution sooner than the maximum-spanning-tree (MST) baseline, including on the paper's noise-corrupted variants. Reproduced from Figure 10 of the published paper.</figcaption>
</figure>
<br />

The representative-dataset study shows the same overall pattern on the Intel $SE(2)$ pose graph, the Rim $SE(3)$ dataset, Pantheon rotation averaging, and a $Gal(3)$ Drone graph derived from a real drone sequence but populated with synthetic relative measurements. The method's value is therefore not that its linear solve is always the cheapest initialization. Its value is that **initialization plus refinement** can be faster and more reliable than beginning with a poorer estimate.

## Boundaries and open questions

“Any matrix Lie group” describes the reusable linear-algebraic template. It does not mean that every group is automatically handled without additional work. A new group still needs a usable matrix representation, a projection $\Pi_G$, and appropriate manifold operations and factors for refinement.

FAST-Sync itself does not provide a global-optimality certificate. The experiments show that it often places a local optimizer in the reference solution's basin, with the reference distinction described above, but that is an empirical result. The paper studies its isotropic Gaussian model and additional injected noise; it does not evaluate gross-outlier robustness. Robust losses, outlier rejection, or richer covariance models in a subsequent GTSAM refinement are reasonable engineering extensions, but they are outside the experiments reported in the paper.

## Takeaway

FAST-Sync turns group synchronization into a strong starting estimate by combining five ideas: an ambient Frobenius surrogate, Kronecker reduction, block-preserving Nested Dissection, sparse back-substitution, and projection to the group. The result is not the end of the optimization pipeline. It gives a local optimizer a stronger starting point and, in many of the reported experiments, reduces total initialization-plus-refinement runtime.

Further browsing:

- [FAST-Sync paper](https://doi.org/10.1109/LRA.2026.3710327)
- [GTSAM repository](https://github.com/borglab/gtsam)
- [SE-Sync](https://github.com/david-m-rosen/SE-Sync), for certifiable $SE(d)$ synchronization
- [VGGT-SLAM on the $SL(4)$ manifold]({% post_url 2026-06-24-vggt-slam %}), for a recent GTSAM application beyond rigid poses

<!-- Editorial note: Figures 1, 3, and 10 are reproduced from the published IEEE RA-L article. Confirm IEEE republication permission before public posting. -->
